{
  "$defs": {
    "BudgetCeiling": {
      "additionalProperties": false,
      "description": "The ceilings a manifest sets on any scan it authorizes.",
      "properties": {
        "maximum_packets": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "maximum_packets_per_second": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "maximum_runtime_seconds": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "maximum_packets",
        "maximum_runtime_seconds",
        "maximum_packets_per_second"
      ],
      "type": "object"
    },
    "PolicyDecisionTag": {
      "enum": [
        "approved",
        "denied"
      ],
      "type": "string"
    },
    "ScopeId": {
      "description": "Identifies an authorization scope manifest.",
      "pattern": "^scope_[0-7][0-9A-HJKMNP-TV-Z]{25}$",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "budget_ceiling": {
      "$ref": "#/$defs/BudgetCeiling"
    },
    "decision": {
      "$ref": "#/$defs/PolicyDecisionTag",
      "description": "`approved` only if the manifest is usable *and* every supplied target\nis inside its allow set and outside its deny set."
    },
    "description": {
      "type": "string"
    },
    "detail": {
      "type": [
        "string",
        "null"
      ]
    },
    "expired": {
      "description": "True when the manifest's expiry has passed. An expired manifest\nauthorizes nothing.",
      "type": "boolean"
    },
    "in_scope_count": {
      "description": "How many of the supplied targets, after expansion, are inside the\nallow set and outside the deny set.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "out_of_scope": {
      "description": "The supplied targets that are not, as written by the caller.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "reason_code": {
      "description": "A stable code, present exactly when `decision` is `denied`.",
      "type": [
        "string",
        "null"
      ]
    },
    "scope_id": {
      "$ref": "#/$defs/ScopeId",
      "description": "The identity this manifest declares. It is what a started scan\nrecords on every event it writes."
    },
    "signature_present": {
      "description": "True when the document carries a signature field. Signatures are not\nverified in this version, so a present one grants nothing.",
      "type": "boolean"
    },
    "signature_verified": {
      "type": "boolean"
    }
  },
  "required": [
    "decision",
    "scope_id",
    "description",
    "expired",
    "in_scope_count",
    "out_of_scope",
    "budget_ceiling",
    "signature_present",
    "signature_verified"
  ],
  "title": "ScopeValidateOutput",
  "type": "object"
}
